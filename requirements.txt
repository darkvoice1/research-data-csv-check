# 核心后端框架（必选，支撑所有接口开发）
flask==2.3.3

# 跨域支持（必选，解决前端GitHub Pages调用后端的跨域问题）
flask-cors==4.0.0

# CSV解析工具（必选，比Python内置csv库更易处理编码/格式问题）
pandas==2.1.4

# 可选：环境变量管理（新手友好，避免硬编码配置，如上传路径/端口）
# python-dotenv==1.0.0